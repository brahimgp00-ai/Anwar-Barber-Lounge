export interface ServiceItem {
  id: string;
  number: string;
  name: string;
  description: string;
}

export interface GalleryProject {
  id: string;
  number: string;
  name: string;
  category: string;
  images: string[];
}
